# ⚠️ TEMPLATE FILE - DO NOT COMMIT WITH REAL SECRETS
# This is an example file for local development environment variables

# ============================================================================
# HOW TO USE THIS FILE:
# ============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your local secrets (NOT this file)
# 3. Never commit .env to git (it's already gitignored)
# ============================================================================

# REQUIRED: Generate a strong secret key for local development
# Generate: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CHANGE-THIS-TO-STRONG-SECRET-KEY-DO-NOT-COMMIT-REAL-VALUE

# OPTIONAL: SQLite database path for local development
SQLITE_DATABASE_PATH=messaging_app.db

# OPTIONAL: PostgreSQL/CockroachDB connection string (leave empty to use SQLite)
# Format: postgresql://username:password@host:port/database?sslmode=verify-full
# DATABASE_URL=

# REQUIRED: Trusted origins (comma-separated list for local development)
TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# OPTIONAL: Allow connections from LAN IPs (useful for mobile testing)
# This is automatically handled in development mode

# Development mode settings
APP_DEBUG=true
FLASK_ENV=development
