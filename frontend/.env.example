# Example environment configuration
# Copy this file to .env.local to override default settings

# Backend API URL (optional)
# If not set, the app will automatically use the current hostname with the configured port
# This is useful for production deployments or custom backend URLs
# REACT_APP_API_URL=http://192.168.1.100:5000

# Backend API Port (optional, defaults to 5000)
# Only used when REACT_APP_API_URL is not set
# The app will automatically use the page's protocol (http: or https:)
# REACT_APP_API_PORT=5000

# For localhost development (default behavior):
# The app automatically uses: protocol://localhost:5000
# No configuration needed!

# For network access (example - replace with your actual IP):
# REACT_APP_API_URL=http://192.168.1.50:5000

# For custom port:
# REACT_APP_API_PORT=8000

# For production deployment with HTTPS:
# REACT_APP_API_URL=https://your-backend-domain.com
# or if using same domain with different port:
# REACT_APP_API_PORT=443
